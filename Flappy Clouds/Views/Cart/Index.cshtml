@* @model Flappy_Clouds.ViewModels.CartViewModel

@{
    ViewData["Title"] = "Your Cart";
}

<h1>Your Cart</h1>

<table>
    <thead>
        <tr>
            <th>Product</th>
            <th>Quantity</th>
            <th>Price</th>
            <th>Total</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Items)
        {
            <tr>
                <td>@item.ProductName</td>
                <td>
                    <input type="number" name="Quantities[@item.ProductId]" value="@item.Quantity" min="1" />
                </td>
                <td>@item.Price.ToString("C")</td>
                <td>@item.Total.ToString("C")</td>
            </tr>
        }
    </tbody>
</table>

<h3>Cart Total: @Model.CartTotal.ToString("C")</h3>

<hr />

<h2>Payment Options</h2>
<form method="post" asp-action="Checkout">

    <div>
        <input type="radio" id="cod" name="PaymentMethod" value="CashOnDelivery" checked />
        <label for="cod">Cash on Delivery</label>

        <input type="radio" id="sslcommerz" name="PaymentMethod" value="OnlinePayment" />
        <label for="sslcommerz">Online Payment</label>
    </div>

    @if (!Model.IsLoggedIn)
    {
        <h2>Customer Information</h2>
        <div>
            <label>Name</label>
            <input type="text" name="CustomerName" required />
        </div>
        <div>
            <label>Phone Number</label>
            <input type="text" name="PhoneNumber" required />
        </div>
        <div>
            <label>Address</label>
            <textarea name="Address" required></textarea>
        </div>
    }

    <button type="submit">Place Order</button>
</form>
 *@